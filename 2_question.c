#include <stdio.h>
int main() {
    int a = 1, b = 0;

    printf("a AND b = %d\n", a && b);
    printf("a OR b = %d\n", a || b);
    printf("NOT a = %d\n", !a);
    printf("NOTb = %d\n", !b);

    return 0;
}
